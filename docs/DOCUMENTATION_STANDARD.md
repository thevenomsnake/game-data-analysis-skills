# Documentation Standard

Documentation describes public behavior and points to the machine authority. Keep configuration
and schemas in JSON, lifecycle procedures in `sql-engineering/references/`, and human workflows
in `docs/`.

Use repository-relative links. Do not record personal absolute paths, private hosts, credentials,
or production result examples. When a public command changes, update the relevant README,
reference, and focused test together.
