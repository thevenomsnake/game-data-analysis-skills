interface:
  display_name: "SQL Engineering"
  short_description: "Save, version, find, and deliver SQL files"
  default_prompt: "Use $sql-engineering to create or modify SQL as an indexed, immutable project file and return its exact delivery path."
