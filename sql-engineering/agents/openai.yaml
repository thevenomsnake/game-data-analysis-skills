interface:
  display_name: "SQL Engineering"
  short_description: "Initialize and govern planning sources, SQL, assets, and delivery"
  default_prompt: "Use $sql-engineering to route local project setup, governed game-data SQL, reference lookup, result workflow, review, and formal asset requests through the public lifecycle."
