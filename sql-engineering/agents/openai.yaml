interface:
  display_name: "SQL Engineering"
  short_description: "Version SQL and run it through configured databases"
  default_prompt: "Use $sql-engineering to save SQL as an indexed immutable project file, run it through the configured read-only database environment when available, or return the exact SQL path for manual execution."
